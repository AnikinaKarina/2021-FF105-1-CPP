#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    float x,f;
    cin >> x;
    setlocale(0, "");
    if (x == 0 || x == sqrt(15) || x == sqrt(-15)) cout << "деление на ноль";
    else cout << abs(pow(x, 2) - pow(x, 3)) - 7 * x / (pow(x, 3) - 15 * x);
    return 0;
}

